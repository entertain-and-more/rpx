# TASKPLAN Readback — Task 1899 — RPX Source-Smoke

Zeitpunkt: 2026-08-12 07:39 Europe/Berlin
Rolle: TASKSOLVER
Modus: lokaler Verify-/Remote-Readback; kein Push, kein Force-Push

## Sollzustand

Task 1899 fordert einen belastbaren macOS/Linux-Source-Smoke-Nachweis für den
Commit `b0c2173551fe3b44828f96e8f305acf5cc0b6122`, einschließlich Remote-CI
auf Ubuntu und macOS. Ein Push des Ahead-Slices ist nur bei ausdrücklicher
User-/Maintainerfreigabe zulässig.

## Aktueller Git-Befund

```text
HEAD         4b017089555136c88dcc6799321e0f036df393f3
origin/master 3e35863233af76729a4d8004bd5a595fb4b5d419
status       master...origin/master [ahead 9, behind 2]
```

Der angeforderte Commit `b0c2173` ist im lokalen Verlauf vorhanden und ein
Vorfahre von `HEAD`; er ist nicht mehr der aktuelle lokale Ahead-Slice. Das
lokale Ahead-Segment enthält neun lokale Dokumentations-/Release-Commits,
während `origin/master` zwei spätere CI-Commits enthält. Insbesondere enthält
die lokale Workflow-Datei die Linux-Qt-Runtime-Installation aus dem Remote-
Stand nicht. Ein normaler Push wäre daher nicht fast-forward-fähig und würde
zuvor eine nicht autorisierte Integration erfordern.

Der Arbeitsbaum enthält außerdem fremde/ungeklärte Änderungen ausschließlich
unter `web_companion/` (geänderte und neue Icon-/Manifest-Dateien). Diese
Dateien wurden nicht angefasst, gestaged oder veröffentlicht.

## Lokaler Source-Smoke

Auf Windows 11 (Python 3.12.10, PySide6 6.11.1, pytest 9.1.1; `pygame` nicht
installiert) wurde aus dem lokalen Clone ausgeführt:

```text
python -m pytest -q tests/test_source_platform_smoke.py
.....                                                                    [100%]
```

Ergebnis: **5 passed**. Das ist ein lokaler Quell-/Import-/Compile-Nachweis;
es ist kein macOS- oder Linux-Lauf und keine manuelle Desktop-, Audio-,
Dateidialog- oder Zweitmonitor-Abnahme.

## Remote-CI-Readback

Die GitHub Actions-Läufe sind read-only verifiziert:

- [Run 31047251034](https://github.com/entertain-and-more/rpx/actions/runs/31047251034), SHA `1031c71405bf2d4ee600a7a99bab69ed291f1e0b`: `source-smoke (ubuntu-latest, 3.12)` und `source-smoke (macos-latest, 3.12)` jeweils **success**; Syntax-Matrix 3.10/3.11/3.12 ebenfalls **success**.
- [Run 31049697538](https://github.com/entertain-and-more/rpx/actions/runs/31049697538), SHA `3e35863233af76729a4d8004bd5a595fb4b5d419` (aktueller `origin/master`): beide Source-Smoke-Matrix-Jobs sowie Syntax 3.10/3.11/3.12 **success**.

Damit ist der Remote-CI-Nachweis für Ubuntu und macOS erbracht. Die
Dokumentation beschreibt ausdrücklich, dass CI keine physische Audio-,
Fenster-, Dateidialog- oder Monitor-Abnahme ersetzt.

## Disposition

Der CI-/Source-Smoke-Nachweis ist grün, aber der lokale Branch ist gegenüber
dem Remote nicht direkt veröffentlichbar: zwei Remote-Commits fehlen lokal,
der Ahead-Slice enthält neun lokale Commits, und es gibt ungeklärte fremde
Arbeitsbaumänderungen. Da keine ausdrückliche Push-/Maintainerfreigabe vorliegt,
wurde weder gemergt, rebased, gepusht noch anderweitig extern mutiert. Task
1899 bleibt für die Veröffentlichung als `blocked_dependency` dokumentiert.
