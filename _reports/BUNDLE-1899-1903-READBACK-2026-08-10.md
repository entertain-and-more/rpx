# TASKPLAN bundle readback: 1899 / 1903

Date: 2026-08-10
Project: `C:\_Local_DEV\repos\rpx`
Local commit: `fb714c0fae7760f46d9dbce187f50f3a2d439c35`

## Task 1903 — metadata alignment

Implemented and verified the common application/store version, publisher,
privacy/support URLs, privacy review date, and explicit **Unreleased /
Unveröffentlicht** status across `PRIVACY_POLICY.md`, `store_package.json`,
`store_package/RPX Pro/AppxManifest.xml`, `STORE_LISTING.md`, `README.md`, and
`README_de.md`. The AppxManifest descriptions were also made well-formed XML.

Verification:

- `python -X utf8 _scripts/verify_release_metadata.py` — passed
- `python -X utf8 -m pytest -q` — 15 passed
- `python -X utf8 -m compileall -q rpx_pro` — passed

No legal, Store, release, or publication approval was inferred.

## Task 1899 — local macOS/Linux source-smoke slice

The local source-smoke suite passed (5 tests) and the cross-platform workflow
definition remains present. Remote Actions runs for commits `3e35863` and
`1031c71` are successful, but the live remote head is
`3e35863233af76729a4d8004bd5a595fb4b5d419`, not this local commit. The local
slice at `fb714c0fae7760f46d9dbce187f50f3a2d439c35` has no live CI result.

Status remains **open**: publishing it requires an authorised push and
maintainer approval. No push, force-push, or GitHub mutation was performed.
Manual macOS/Linux desktop checks are also not claimed.

The selector returned task 1899 again after this read-only retry without any
new authorization or remote-state change. The project cursor was therefore
advanced with `python -m taskplan skip --role tasksolver --project
"C:\\_Local_DEV\\repos\\rpx"`; this does not mark the task done and preserves
the blocker for a later authorized continuation.

The detailed local evidence note is kept at
`docs/evidence/source-smoke/2026-08-10-local-windows-python312.md` (ignored by
the repository's existing `docs/` rule and therefore intentionally not staged).
